## **Suggestions**

- When app starts it should start in Map mode (3 users)
- Story/Blog interface should be side by side, not vertical (2 users)
- Historical Places should be in another toggelable view mode of the page, should not be on the bottom of the page as it is now. (1 users) -- too much clutter on screen, not easy to use
- The community stories should be saved somehow. We should be able to load them on startup. Also when a user submits a story, it should be added to storage. -- This process may be changed because we need to deploy the final app to vercel. (3 users)
- Locations, when opened, should also display some images from the internet. These should be related to the location. If no image is found, it should display a placeholder image. (2 users)